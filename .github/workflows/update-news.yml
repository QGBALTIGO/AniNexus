name: Atualizar notícias

on:
  workflow_dispatch:
  schedule:
    - cron: '17 * * * *'
  push:
    paths:
      - 'scripts/update-news-v35.mjs'
      - 'lib/news-sources-v35.mjs'
      - 'lib/news-sources-v34.mjs'
      - 'lib/news-core.mjs'
      - '.github/workflows/update-news.yml'

permissions:
  contents: write

concurrency:
  group: aninexus-news-v35
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Validar coletor V35
        run: |
          node --check lib/news-sources-v35.mjs
          node --check scripts/update-news-v35.mjs
      - name: Buscar JBox, Crunchyroll e fontes complementares
        env:
          NEWS_RETENTION_DAYS: '5'
          NEWS_MAX_SOURCE_AGE_HOURS: '120'
          NEWS_MIN_QUALITY: '0.52'
        run: node scripts/update-news-v35.mjs
      - name: Validar feed V35
        run: node tests/news-system.mjs
      - name: Publicar feed interno
        run: |
          if git diff --quiet -- data/news.json; then
            echo "Sem mudanças no feed."
            exit 0
          fi
          git config user.name "AniNexus News Bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/news.json
          git commit -m "chore: atualizar notícias V35 [skip ci]"
          git push
